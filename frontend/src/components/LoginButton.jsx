function LoginButton() {
    return (
        <button
            type="submit"
            className="w-full h-[50px] bg-white text-slate-900 rounded-xl text-sm font-semibold hover:bg-slate-100 active:scale-[0.98] transition-all mt-4 cursor-pointer shadow-lg"
        >
            Masuk
        </button>
    );
}

export default LoginButton;